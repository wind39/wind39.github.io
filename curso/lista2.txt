#Curso de Programação C# 
## William Ivanski 2014

###Lista de Exercícios Capítulo 2 (Respostas)
---

1) Solicite dois números ao usuário e imprima a __soma__ dos dois números.

    // declarar 3 variaveis, a, b, e c
    int a, b, c;
    
    // pedir para o usuario digitar o valor da variavel A
    Console.Write("Digite o valor da variavel A: ");
    a = Int32.Parse(Console.ReadLine());

    // pedir para o usuario digitar o valor da variavel B
    Console.Write("Digite o valor da variavel B: ");
    b = Int32.Parse(Console.ReadLine());

    // calcular a operação e jogar o resultado em C
    c = a + b;

    // mostrar o valor de C para o usuario
    Console.WriteLine("A soma de A e B = " + c);
    Console.ReadKey();

2) Solicite dois números ao usuário e imprima a __subtração__ dos dois números.

    int a, b, c;
    
    Console.Write("Digite o valor da variavel A: ");
    a = Int32.Parse(Console.ReadLine());
    
    Console.Write("Digite o valor da variavel B: ");
    b = Int32.Parse(Console.ReadLine());

    c = a - b;

    Console.WriteLine("A subtração de A e B = " + c);
    Console.ReadKey();

3) Solicite dois números ao usuário e imprima a __multiplicação__ dos dois números.

    int a, b, c;
    
    Console.Write("Digite o valor da variavel A: ");
    a = Int32.Parse(Console.ReadLine());
    
    Console.Write("Digite o valor da variavel B: ");
    b = Int32.Parse(Console.ReadLine());

    c = a * b;

    Console.WriteLine("A multiplicação de A e B = " + c);
    Console.ReadKey();

4) Solicite dois números ao usuário e imprima a __divisão__ dos dois números.

    int a, b;
    double c;
    
    Console.Write("Digite o valor da variavel A: ");
    a = Int32.Parse(Console.ReadLine());
    
    Console.Write("Digite o valor da variavel B: ");
    b = Int32.Parse(Console.ReadLine());

    c = (double) a / (double) b;

    Console.WriteLine("A divisão de A e B = " + c);
    Console.ReadKey();

5) Solicite um número ao usuário e imprima o seu __dobro__.

    int var, dobro;
            
    Console.Write("Digite um número inteiro: ");
    var = Int32.Parse(Console.ReadLine());

    dobro = var * 2;

    Console.WriteLine("O dobro do número que você digitou é: " + dobro);
    Console.ReadKey();

6) Solicite um número ao usuário e imprima o seu __quadrado__.

    int var, quadrado;
            
    Console.Write("Digite um número inteiro: ");
    var = Int32.Parse(Console.ReadLine());

    dobro = var * var;

    Console.WriteLine("O quadrado do número que você digitou é: " + quadrado);
    Console.ReadKey();

7) __Cadastro de aluno__. Solicite ao usuário e imprima os seguintes valores:

Nome (string)
Idade (int)
Sexo (char)
Nota média final (double)

    string nome;
    int idade;
    char sexo;
    double nota;
    
    Console.Write("Digite o nome do aluno: ");
    nome = Console.ReadLine();

    Console.Write("Digite a idade do aluno: ");
    idade = Int32.Parse(Console.ReadLine());

    Console.Write("Digite o sexo do aluno (M ou F): ");
    sexo = Char.Parse(Console.ReadLine());

    Console.Write("Digite a média final do aluno no curso: ");
    nota = Double.Parse(Console.ReadLine());

    Console.WriteLine("O aluno " + nome + " tem " + idade + " anos, é do sexo " + sexo + ", e obteve a nota final = " + nota + ".");
    Console.ReadKey();

8) __Conversor de tempo__. Solicite ao usuário o total em segundos, e imprima o correspondente em horas, minutos e segundos.

    int segundos;
    int minutos;
    int horas;

    Console.Write("Digite o total em segundos: ");
    segundos = Int32.Parse(Console.ReadLine());

    horas = segundos / 3600;
    segundos = segundos - (horas * 3600);

    minutos = segundos / 60;
    segundos = segundos - (minutos * 60);

    Console.WriteLine(horas + " horas, " + minutos + " minutos e " + totalsegundos + " segundos");
    Console.ReadKey();

9) Imprimir sequências de inteiros.

    int x;

    x = 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    x = x + 1;
    Console.WriteLine(x);

    Console.ReadKey();

10) Imprimir tabuadas.

    int x, tabuada;

    Console.Write("Digite a tabuada desejada: ");
    tabuada = Int32.Parse(Console.ReadLine());

    x = 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));
            
    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    x = x + 1;
    Console.WriteLine(tabuada + " x " + x + " = " + (tabuada * x));

    Console.ReadKey();
