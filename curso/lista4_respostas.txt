#Curso de Programação C# 
## William Ivanski 2014

###Lista de Exercícios Capítulo 4
---

1) Modifique o exercício __2.9__ (sequências de inteiros) para imprimir _N_ inteiros, sendo _N_ um número informado pelo usuário.

    int i; // variavel contadora (ou de incremento)
    int n; // variavel que armazena o numero de vezes

    // pedindo o valor de N para o usuário
    Console.Write("Digite o valor de N: ");
    n = Int32.Parse(Console.ReadLine());

    i = 1; // inicializacao da variavel

    while (i <= n) // enquanto x for menor ou igual a n, faça:
    {
        Console.WriteLine(i); // imprimindo o valor da variavel
        i = i + 1; // incrementando a variavel
    }

    Console.ReadKey();

2) Utilize _while_ no exercício __2.10__ (tabuadas).

    int tabuada; // variavel que armazena a tabuada desejada
    int n; // variavel que armazena o numero de vezes
    int i; // variavel de incremento

    // solicitando a tabuada ao usuario
    Console.Write("Digite a tabuada desejada: ");
    tabuada = Int32.Parse(Console.ReadLine());

    // solicitando o numero de vezes ao usuario
    Console.Write("Digite o número de vezes: ");
    n = Int32.Parse(Console.ReadLine());

    i = 1; // inicializando a variavel de incremento

    while (i <= n) // enquanto i for menor ou igual a n
    {
        Console.WriteLine("{0} x {1} = {2}", tabuada, i, (tabuada * i));
        i = i + 1;
    }

    Console.ReadKey();

3) Modifique o exercício __3.8__ (cadastro de aluno) para tratar para, caso o usuário digite valores inválidos no sexo e na nota das provas, o programar informar o erro e solicitar o valor novamente, até que o usuário digite um valor válido.

    string nome;
    int idade;
    char sexo;
    string sexoporextenso; // conterá "masculino" ou "feminino"
    double nota1, nota2, nota3, media;
    string situacao; // conterá "aprovado" ou "reprovado"

    Console.Write("Digite o nome do aluno: ");
    nome = Console.ReadLine();

    Console.Write("Digite a idade do aluno: ");
    idade = Int32.Parse(Console.ReadLine());

    // solicitando a variavel sexo pela primeira vez
    Console.Write("Digite o sexo do aluno (M ou F): ");
    sexo = Char.Parse(Console.ReadLine());

    // garantindo que o usuario vai digitar um valor valido para a variavel sexo
    while (! (sexo == 'M' || sexo == 'm' || sexo == 'F' || sexo == 'f'))
    {
        Console.WriteLine("ERRO! Sexo {0} inválido.", sexo);

        Console.Write("Digite o sexo do aluno (M ou F): ");
        sexo = Char.Parse(Console.ReadLine());
    }

    // solicitando nota da prova 1
    Console.Write("Digite a nota da prova 1: ");
    nota1 = Double.Parse(Console.ReadLine());

    // garantindo que a nota da prova 1 é válida
    while (! (nota1 >= 0 && nota1 <= 10))
    {
        Console.WriteLine("ERRO! Nota {0} inválida.", nota1);

        Console.Write("Digite a nota da prova 1: ");
        nota1 = Double.Parse(Console.ReadLine());
    }

    // solicitando nota da prova 2
    Console.Write("Digite a nota da prova 2: ");
    nota2 = Double.Parse(Console.ReadLine());

    // garantindo que a nota da prova 2 é válida
    while (! (nota2 >= 0 && nota2 <= 10))
    {
        Console.WriteLine("ERRO! Nota {0} inválida.", nota2);

        Console.Write("Digite a nota da prova 2: ");
        nota2 = Double.Parse(Console.ReadLine());
    }

    // solicitando nota da prova 3
    Console.Write("Digite a nota da prova 3: ");
    nota3 = Double.Parse(Console.ReadLine());

    // garantindo que a nota da prova 3 é válida
    while (! (nota3 >= 0 && nota3 <= 10))
    {
        Console.WriteLine("ERRO! Nota {0} inválida.", nota3);

        Console.Write("Digite a nota da prova 3: ");
        nota3 = Double.Parse(Console.ReadLine());
    }

    // USUARIO DIGITOU TODAS AS VARIAVEIS CORRETAMENTE!!!

    // calculando a media ponderada
    media = ((1 * nota1) + (2 * nota2) + (3 * nota3)) / 6.0;

    // sexo por extenso
    if (sexo == 'M' || sexo == 'm')
    {
        sexoporextenso = "masculino";
    }
    else
    {
        sexoporextenso = "feminino";
    }

    // situacao por extenso
    if (media >= 7)
    {
        situacao = "aprovado";
    }
    else
    {
        situacao = "reprovado";
    }

    Console.WriteLine("O aluno {0} tem {1} anos, é do sexo {2} e obteve a nota final = {3}, situação {4}.", nome, idade, sexoporextenso, media, situacao);

    Console.ReadKey();

4) Dada uma coleção de números naturais terminada por 0, imprimir seus quadrados.

    int n;

    Console.WriteLine("Digite números para obter seus quadrados.");
    Console.WriteLine("Quando quiser parar, digite 0.");

    do
    {
        // solicitando o número ao usuário
        n = Int32.Parse(Console.ReadLine());

        if (n > 0)
        {
            // imprimindo o quadrado
            Console.WriteLine("O quadrado de {0} é {1}.", n, (n * n));
        }
        else
        {
            if (n < 0)
            {
        // informando erro
        Console.WriteLine("Erro! O número deve ser positivo.");
            }
        }
    }
    while (n != 0);

    Console.WriteLine("Pressione qualquer tecla para encerrar.");
    Console.ReadKey();

5) Dado _N_, calcular a soma dos _N_ primeiros números naturais.

    int n;
    int soma;

    // solicitando ao usuário o valor de n
    Console.Write("Digite o valor de N: ");
    n = Int32.Parse(Console.ReadLine());

    // Exemplos:
    // - Para N = 1: 0                 = 0
    // - Para N = 2: 0 + 1             = 1
    // - Para N = 3: 0 + 1 + 2         = 3
    // - Para N = 4: 0 + 1 + 2 + 3     = 6
    // - Para N = 5: 0 + 1 + 2 + 3 + 4 = 10

    soma = 0;

    for (int i = 0; i < n; i++)
        soma += i;

    Console.WriteLine("A soma dos {0} primeiros números naturais é igual a {1}", n, soma);
    Console.ReadKey();

6) Dado _N_, imprimir os _N_ primeiros naturais ímpares. Exemplo, para _N_ = 4, a saída deverá ser 1, 3, 5, 7.

    int n;
    int impar;
    int soma;

    // solicitando N ao usuário
    Console.Write("Digite o valor de N: ");
    n = Int32.Parse(Console.ReadLine());

    soma = 1;

    impar = 1;
    Console.Write("{0}", impar);
    impar += 2;

    for (int i = 1; i < n; i++)
    {
        Console.Write(", {0}", impar);
        soma += impar;
        impar += 2;
    }

    Console.WriteLine();
    Console.WriteLine("A soma dos {0} primeiros números ímpares é igual a {1}.", n, soma);
    Console.ReadKey();

7) Durante os 30 dias do mês de abril foram tomadas as temperaturas médias diárias de Euclides da Cunha (BA). Determinar o número de dias desse mês com temperaturas abaixo de zero.

    int temperatura; // temperatura média diária informada pelo usuário
    int numdias; // numero de dias com temperatura média abaixo de zero

    numdias = 0;

    for (int i = 1; i <= 30; i++)
    {
        // solicitando ao usuário a temperatura do dia
        Console.Write("Digite a temperatura do dia {0}: ", i);
        temperatura = Int32.Parse(Console.ReadLine());

        // verificando se a temperatura é abaixo de zero
        if (temperatura < 0)
            numdias++;
    }

    Console.WriteLine("O número de dias com temperatura média abaixo de zero foi {0}.", numdias);
    Console.ReadKey();

8) Dados _x_ inteiro e _n_ natural, calcular _x^n^_.

    int x;
    int n;
    int resultado;

    // solicitando o valor de x
    Console.Write("Digite o valor de X: ");
    x = Int32.Parse(Console.ReadLine());

    // solicitando o valor de n
    do
    {
        Console.Write("Digite o valor de N: ");
        n = Int32.Parse(Console.ReadLine());

        if (n < 0)
            Console.WriteLine("ERRO! N não pode ser negativo.");
    }
    while (n < 0);

    resultado = 1;

    for (int i = 0; i < n; i++)
        resultado *= x;

    Console.WriteLine("{0} elevado a {1} é igual a {2}.", x, n, resultado);
    Console.ReadKey();

9) Uma loja de discos anota diariamente durante o mês de março a quantidade de discos vendidos. Determinar em que dia desse mês ocorreu a maior venda e qual foi a quantidade de discos vendida nesse dia.

    int venda;
    int maiorvenda;
    int diamaiorvenda;

    // solicitando ao usuário o valor da venda do primeiro dia
    Console.Write("Digite a quantidade de discos vendidos no dia 1: ");
    venda = Int32.Parse(Console.ReadLine());

    maiorvenda = venda;
    diamaiorvenda = 1;

    for (int i = 2; i <= 31; i++)
    {
        // solicitando ao usuário o valor da venda do primeiro dia
        Console.Write("Digite a quantidade de discos vendidos no dia {0}: ", i);
        venda = Int32.Parse(Console.ReadLine());

        if (venda > maiorvenda)
        {
            maiorvenda = venda;
            diamaiorvenda = i;
        }
    }

    Console.WriteLine("A maior venda ocorreu no dia {0}, e o número de discos vendidos nesse dia foi {1}.", diamaiorvenda, maiorvenda);
    Console.ReadKey();

10) Dados o número _N_ de alunos de uma turma de __Programação em C#__ e suas notas da primeira prova, determinar a maior e a menor nota obtidas por essa turma (nota máxima = 100 e nota mínima = 0).

11) Dados _N_ e uma sequência de _N_ números inteiros, determinar a soma dos números pares.

12) Dado _n_ natural, determinar _n!_.

13) Dado _N_ e dois números naturais _i_ e _j_ diferentes de 0, imprimir em ordem crescente os _N_ primeiros naturais que são múltiplos de _i_ ou de _j_ ou de ambos. Exemplo, para _N_ = 6, _i_ = 2 e _j_ = 3, a saída deverá ser: 0, 2, 3, 4, 6, 8.

14) Dizemos que um número natural é _triangular_ se ele é produto de três números naturais consecutivos. Exemplo, 120 é triangular, pois 4.5.6 = 120. Dado _N_ natural, verificar se _N_ é triangular.

15) Dizemos que um número natural é _primo_ se ele é divisível apenas por 1 e por ele mesmo. Dado _p_ natural, verificar se _p_ é primo.

16) Dados dois números inteiros positivos, determinar o máximo divisor comum entre eles usando o algoritmo de Euclides. Exemplo:

![](http://williamivanski.com.br/curso/euclides.png) 

O mdc entre 1128 e 336 é 24.

17) Dado _N_ inteiro positivo, dizemos que _N_ é _perfeito_ se for igual à soma de seus divisores positivos diferentes de _N_.
Exemplo: 6 é perfeito, pois 1 + 2 + 3 = 6.
Verificar se um dado número inteiro positivo é perfeito.

18) Um matemático italiano da Idade Média conseguiu estudar o número de galhos em um certo nível de uma árvore através de uma sequência de números naturais que passou a ser conhecida como __sequência de Fibonacci__. O _n-ésimo_ número da sequência de Fibonacci _F~n~_ é dado pela seguinte fórmula de recorrência:
_F~1~_ = 1
_F~2~_ = 1
_F~i~_ = _F~i-1~_ + _F~i-2~_ para i >= 3.
Faça um programa que dado _n_ calcule _F~n~_.

19) Dizemos que um número _i_ é congruente módulo _m_ a _j_ se _i_ mod _m_ = _j_ mod _m_.
Exemplo: 35 é congruente módulo 4 a 39, pois 35 mod 4 = 3 = 39 mod 4.
Dados _N_, _j_ e _m_ naturais não nulos, imprimir os _N_ primeiros naturais congruentes a _j_ módulo _m_.

20) Dado um número natural na base binária, transformá-lo para a base decimal.
Exemplo, dado 10010 a saída será 18, pois 1.2^4^+0.2^3^+0.2^2^+1.2^1^+0.2^0^ = 18.

21) Dado um número natural na base decimal, transformá-lo para a base binária.
Exemplo, dado 18 a saída deverá ser 10010.

22) Dados três números naturais, verificar se eles formam os lados de um triângulo retângulo.

23) Dados três números, imprimi-los em ordem crescente.

24) Qualquer número natural de quatro algarismos pode ser dividido em duas dezenas formadas pelos seus dois primeiros e dois últimos dígitos.
Exemplos: 1297 (12 e 97) e 5314 (53 e 14).
Escreva um programa que imprima todos os milhares (4 algarismos) cuja raiz quadrada seja a soma das dezenas formadas pela divisão acima.
Exemplo: raiz de 9801 = 99 = 98 + 01. Portanto, 9801 é um dos números a ser impresso.

25) Dados _N_ e uma sequência de _N_ números inteiros, determinar quantos segmentos iguais consecutivos compõem essa sequência.
Exemplo: a sequência 5,2,2,3,4,4,4,4,1,1 é formada por 5 segmentos de números iguais.

26) Dados um inteiro positivo _N_ e uma sequência de _N_ números inteiros, determinar o comprimento de um segmento crescente de comprimento máximo.
Exemplos:
- Na sequência 5,10,3,__2,4,7,9__,8,5 o comprimento do segmento crescente máximo é 4.
- Na sequência 10,8,7,5,2 o comprimento de um segmento crescente máximo é 1.

27) Simule a execução do programa abaixo destacando a sua saída:

    int a, b, total, soma, termo;

    a = Int32.Parse(Console.ReadLine());
    b = Int32.Parse(Console.ReadLine());

    Console.WriteLine("({0},{1})", a, b);

    total = 0;
    soma = 0;

    while (a != 0)
    {
        total++;
        termo = 1;

        for (int i = 1; i <= b; i++)
            termo *= a;

        Console.WriteLine("Resp = {0}", termo);

        soma += termo;

        Console.WriteLine("Soma = {0}", soma);

        a = Int32.Parse(Console.ReadLine());
        b = Int32.Parse(Console.ReadLine());

        Console.WriteLine("({0},{1})", a, b);
    }

    Console.WriteLine("Total de pares = {0}", total);
    Console.ReadKey();

Dados:
2 3
5 2
7 1
0 5
3 2